import { View } from 'react-native';

// This is a dummy screen for the central intercom button in the tab bar
// The actual intercom functionality is handled by the IntercomModal
export default function IntercomScreen() {
  return <View />;
}
